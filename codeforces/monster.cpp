#include<bits/stdc++.h>
using namespace std;
int main()
{int t;
    cin>>t;
    while(t>0)
    {
        int n;
        vector<int>a,b;set<int>s;
        for(int i=0;i<n;i++)
        {
            int k;
            cin>>k;
            a.push_back(k);s.insert(k);
        }
        
        for(int i=0;i<n;i++)
        {
            int k;
            cin>>k;
            b.push_back(k);
        }
        sort(a.begin(),a.end());
        int x=
        
    }
}